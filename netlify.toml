[build]
  command = "npm run build"
  publish = "dist/expediente-astrid/browser"

[build.environment]
  NODE_VERSION = "22"

# Cualquier ruta la resuelve el router de Angular.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
